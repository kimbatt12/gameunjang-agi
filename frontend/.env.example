# Public frontend environment variables only.
# Keep frontend env files limited to public configuration; store server secrets and private API keys in backend/deployment secret stores.
PUBLIC_APP_BASE_URL=https://example.invalid
